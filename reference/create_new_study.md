# Create a new study in the database

Create a new study in the database

## Usage

``` r
create_new_study(df)
```

## Arguments

- df:

  A data frame with one row containing study details: type, title,
  description, pkstudy (logical), subject_type

## Value

A data frame with the created study details including generated id,
start_date, and status
