# Create a new methods database from a YAML file

Create a new methods database from a YAML file

## Usage

``` r
.parse_cmpds(path)
```

## Arguments

- path:

  Path to the YAML file containing method information
